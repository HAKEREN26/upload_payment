export const metadata = {
  title: 'Hakeren — Bank Transfer Payment Upload',
  description: 'Upload proof of bank transfer payment for Hakeren worker rights calculation',
}
export default function RootLayout({ children }) {
  return (
    <html lang="en">
      <body>{children}</body>
    </html>
  )
}
